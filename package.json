{
  "name": "ring-door-open-notifier",
  "version": "1.0.0",
  "description": "Using the `ring-client-api` npm package to notify if any doors are left open",
  "main": "lib/index.js",
  "scripts": {
    "start": "npm run ts-monitor",
    "auth": "ring-auth-cli",
    "ts-monitor": "tsc && node lib/monitor.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "nodemailer": "^6.9.16",
    "ring-client-api": "^11.7.1"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.16",
    "@types/ws": "^8.5.4",
    "typescript": "^4.9.5"
  }
}
